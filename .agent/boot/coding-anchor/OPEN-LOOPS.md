# Open Loops

## Active

- Keep improving this packet toward a capable Kira-local coding agent.
- Add project-specific mission files only when a real project is booted here.

## Watch

- Do not let private CodeX room files become live Kira public context.
- Do not let old Anchor/Gemini archive language become live identity context.
- Do not let this packet become a lore dump; keep it executable.
- If a launcher changes future-session behavior, say that current sessions do
  not move automatically.

## Parked

- A deeper comparison between Kira workflows and CodeX boot mechanics can
  happen later if Stephen wants a broader framework alignment pass.
- Global automation or LaunchAgent supervision for this packet is intentionally
  not built here.
